bucket       = "yourcompany-terraform-state"   # must match bootstrap's state_bucket_name
region       = "us-east-1"
use_lockfile = true                             # native S3 state locking (Terraform >= 1.10) — no DynamoDB table needed
